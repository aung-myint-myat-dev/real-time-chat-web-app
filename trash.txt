<template>
  <div class="h-screen flex flex-col bg-slate-50 dark:bg-slate-900 text-slate-800 dark:text-slate-100 transition-colors duration-300 overflow-hidden">

    <header
      class="h-16 bg-white border-b border-slate-200 flex items-center justify-between px-4 sm:px-6 z-20 shrink-0">
      <div class="flex items-center space-x-3">
        <button @click="isMobileSidebarOpen = !isMobileSidebarOpen"
          class="p-2 rounded-md text-slate-500 hover:bg-slate-100 md:hidden focus:outline-none">
          <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
        <Link href="/chat" class="flex items-center gap-2.5 group">
          <div
            class="w-9 h-9 rounded-xl bg-brand-500 flex items-center justify-center text-white font-bold text-lg shadow-md shadow-brand-500/20 group-hover:scale-105 transition-transform">
            C
          </div>
          <span class="font-bold text-xl tracking-tight text-slate-900 dark:text-white">
            Check<span class="text-brand-500">Chat</span>
          </span>
        </Link>
      </div>

      <div class="flex items-center space-x-4">
        <div class="flex items-center space-x-2">
          <div
            class="w-9 h-9 rounded-full bg-blue-500 flex items-center justify-center text-white font-semibold shadow-sm">
            <!-- {{ $page.props.auth?.user?.name?.charAt(0).toUpperCase() || 'U' }} -->
            TU
          </div>
          <span class="hidden sm:inline text-sm font-medium text-slate-700">
            <!-- {{ $page.props.auth?.user?.name || 'User' }} -->
            Test User
          </span>
        </div>

        <Link href="/logout" method="post" as="button"
          class="text-xs font-semibold text-slate-500 hover:text-red-600 border border-slate-200 hover:border-red-200 px-3 py-1.5 rounded-md transition bg-white">
          Logout
        </Link>
      </div>
    </header>

    <div class="grow flex relative overflow-hidden">

      <aside class="hidden md:flex flex-col w-80 bg-white border-r border-slate-200 shrink-0">
        <slot name="sidebar" />
      </aside>

      <div v-if="isMobileSidebarOpen" class="md:hidden fixed inset-0 z-40 flex">
        <div @click="isMobileSidebarOpen = false" class="fixed inset-0 bg-slate-600/50 backdrop-blur-xs"></div>

        <aside class="relative flex flex-col w-72 max-w-xs bg-white h-full shadow-xl animate-slide-in">
          <div class="p-4 border-b border-slate-100 flex items-center justify-between">
            <span class="font-bold text-slate-700">Chats list</span>
            <button @click="isMobileSidebarOpen = false" class="text-slate-400 hover:text-slate-600">
              <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
          </div>
          <div class="grow overflow-y-auto" @click="isMobileSidebarOpen = false">
            <slot name="sidebar" />
          </div>
        </aside>
      </div>

      <main class="grow flex flex-col min-w-0 bg-slate-50 relative overflow-hidden">
        <slot />
      </main>

    </div>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { Link } from '@inertiajs/vue3'

const isMobileSidebarOpen = ref(false)
</script>

<style scoped>
.animate-slide-in {
  animation: slideIn 0.2s ease-out forwards;
}

@keyframes slideIn {
  from {
    transform: translateX(-100%);
  }

  to {
    transform: translateX(0);
  }
}
</style>